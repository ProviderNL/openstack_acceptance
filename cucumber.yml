---
default: --tags ~@skip --tags ~@setup --tags ~@cleanup --format pretty
setup: --tags ~@skip --tags @setup --tags ~@cleanup --format pretty
cleanup: --tags ~@skip --tags ~@setup --tags @cleanup --format pretty
